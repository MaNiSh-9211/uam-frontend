# ==============================================================================
# uam-frontend/.env — YOUR credentials (gitignored, never commit)
# cp uam-frontend/.env.example uam-frontend/.env  then fill in values
# ==============================================================================

VITE_API_URL=/api

# nginx runtime proxy to gateway-edge
GATEWAY_PROXY_PASS=
GATEWAY_PROXY_HOST=

UAM_FRONTEND_PORT=8091
